import React from "react";

function Header () {
    const title = "Christi Rose Lato"

    return (
    <div className="header">
        <header> {title} </header>
    </div>
    );
};

export default Header;